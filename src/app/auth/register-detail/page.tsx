// sections
import { RegisterDetailView } from "src/sections/auth";

// ----------------------------------------------------------------------

export const metadata = {
  title: "User Detail Form",
};

export default function RegisterDetailPage() {
  return <RegisterDetailView />;
}
